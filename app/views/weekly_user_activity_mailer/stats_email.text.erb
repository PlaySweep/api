
User Feedback Stats for <%= DateTime.current.to_date %>
===============================================
 
25 new users
==============

<% @new_users.each_with_index do |user, index| %>
  <%= index + 1 %>. <%= user.first_name %> <%= user.last_name %> ( <%= user.email %> ) \r\n
<% end %>

==============

25 users who played two weeks ago and played again the following week

==============
<% @returned_users.each_with_index do |user, index| %>
  <%= index + 1 %>. <%= user.first_name %> <%= user.last_name %> ( <%= user.email %> ) \r\n
<% end %>

==============

25 users who played two weeks ago and didn't return

==============
<% @left_users.each_with_index do |user, index| %>
  <%= index + 1 %>. <%= user.first_name %> <%= user.last_name %> ( <%= user.email %> ) \r\n
<% end %>

==============